<!DOCTYPE html>
<html>
    <head>
        <meta charset="UTF-8">
        <title>{% block title %}Welcome!{% endblock %}</title>
        <link rel="icon" href="data:image/svg+xml,<svg xmlns=%22http://www.w3.org/2000/svg%22 viewBox=%220 0 128 128%22><text y=%221.2em%22 font-size=%2296%22>⚫️</text></svg>">
        {# Run `composer require symfony/webpack-encore-bundle` to start using Symfony UX #}
        {% block stylesheets %}
            <link rel="stylesheet" type="text/css" href="{{ asset('css/style.css') }}">
        {% endblock %}

    </head>
    <body>
        <header>
            <div id="logo">
                <img src="" alt="">
            </div>
            <nav>
                <ul>
                    <li></li>
                    <li></li>
                    <li></li>
                </ul>
            </nav>
            <div>
                {% if app.user %}
                        <a class="user" href="{{ path('app_logout') }}">se déconnecter</a>
                {% else %}
                        <a class="user" href="{{ path('app_register') }}">s'inscrire</a>
                        <a class="user" href="{{ path('app_login') }}">se connecter</a>
                {% endif %}
            </div>
        </header>
        <main>
            {% block body %}{% endblock %}
        </main>
    </body>
</html>
